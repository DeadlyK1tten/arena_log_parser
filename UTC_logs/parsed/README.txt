This directory is where "UTC logs" are moved after being successfully parsed.

To re-run a file, just *move* it back up the "UTC_logs" directory.

(Move it and not copy it so that it can be archived. If a file of the same name is in the "parsed"
directory, the archive operation will fail.)